# Example environment variables - copy this to .env and fill in your values
# For the FIREBASE_SERVICE_ACCOUNT, you can either:
# 1. Put the entire serviceAccountKey.json content as a string
# 2. Or put the path to your serviceAccountKey.json file

# Option 1: JSON content (recommended for deployment)
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "your-project", ...}'

# Option 2: File path (for local development)
# FIREBASE_SERVICE_ACCOUNT=src/trent_agent/config/serviceAccountKey.json